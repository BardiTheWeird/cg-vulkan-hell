#pragma once

// std
#include <string>

namespace lve {
    namespace constants {
        namespace pipeline_keys {
            static inline std::string colored_plain = "colored_plain";
            static inline std::string colored_pbr = "colored_pbr";
            static inline std::string textured_plain = "textured_plain";
            static inline std::string textured_pbr = "textured_pbr";
        }
    }
}
